import styled from 'styled-components';

export const StackContent = () => {
  return <StackContainer></StackContainer>;
};

const StackContainer = styled.section`
  width: 1096px;
  height: 500px;
  padding-bottom: 60px;
`;
